using UnityEngine;

public class EnemyAimDirection : MonoBehaviour
{
    // create an orbit effect that makes the gun rotate around the enemy
    // use trig to offset the gun from the enemy's position
    // instead of attaching the gun object to the enemy as a child, attach it via this script
}